# import openpyxl as op # openpyxl 모듈 import
#
# wb = op.Workbook() # 새로운 Workbook 객체 생성
#
# print(wb) # 객체를 출력해보기
#
# # The r before the string literal in your code tells Python to treat the string as a raw string.
# # This means that Python will not interpret any escape sequences in the string.
# # To prevent unicode error, we put 'r'
# # path = r"/test.xlsx"